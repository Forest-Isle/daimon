## No Capability Changes

This change is a pure cleanup/refactoring effort. No new capabilities are introduced and no existing capability requirements are modified.

All changes are at the implementation level:
- Fix compilation errors
- Remove dead code files
- Update internal type references
- Simplify internal interface

See `design.md` for implementation decisions and `tasks.md` for the work breakdown.
