# 12 - 数据流与交互全景图

## 一、完整消息处理流程

```
┌──────────────────────────────────────────────────────────────────────┐
│                         用户输入                                      │
│                     "帮我写个 Go HTTP 服务"                           │
└────────────────────────────┬─────────────────────────────────────────┘
                             │
                    ┌────────▼────────┐
                    │   Channel 层    │
                    │ Telegram / TUI  │
                    └────────┬────────┘
                             │ InboundMessage
                    ┌────────▼────────┐
                    │    Gateway      │
                    │ handleInbound() │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              │                             │
     ┌────────▼─────────┐        ┌─────────▼──────────┐
     │  Simple Runtime   │        │  Cognitive Agent    │
     │  (mode=simple)    │        │  (mode=cognitive)   │
     └────────┬──────────┘        └─────────┬──────────┘
              │                             │
              │                    ┌────────▼────────┐
              │                    │    PERCEIVE     │ 无 LLM 调用
              │                    │                 │
              │                    │ ·解析意图       │
              │                    │ ·评估复杂度     │
              │                    │ ·检索记忆       │
              │                    │ ·检索知识库     │
              │                    │ ·检索知识图谱   │
              │                    └────────┬────────┘
              │                             │
              │                    ┌────────▼────────┐
              │                    │ 复杂度判断       │
              │                    └──┬─────────┬────┘
              │                       │         │
              │              simple   │         │ moderate/complex
              │         ┌─────────────┘         │
              │         │                       │
              │  ┌──────▼──────┐       ┌───────▼────────┐
              │  │ 委托 Simple │       │     PLAN       │ 1次 LLM 调用
              │  │   Runtime   │       │                │
              │  └─────────────┘       │ ·生成 TaskPlan │
              │                        │ ·子任务分解    │
              │                        │ ·置信度评估    │
              │                        │ ·RL PPO 调整   │
              │                        └───────┬────────┘
              │                                │
     ┌────────▼─────────┐             ┌───────▼────────┐
     │   Agent Loop      │             │      ACT       │ 无 LLM 调用
     │                   │             │                │
     │ 循环:             │             │ ·按依赖执行    │
     │  LLM 调用         │             │ ·工具审批      │
     │  → 文本/工具调用  │             │ ·RL Bandit     │
     │  → 执行工具       │             │ ·记录观察      │
     │  → 下一轮         │             └───────┬────────┘
     │                   │                     │
     └────────┬──────────┘             ┌───────▼────────┐
              │                        │    OBSERVE     │ 无 LLM 调用
              │                        │                │
              │                        │ ·统计成功/失败 │
              │                        │ ·计算进度      │
              │                        │ ·提取错误模式  │
              │                        └───────┬────────┘
              │                                │
              │                        ┌───────▼────────┐
              │                        │    REFLECT     │ 1次 LLM 调用
              │                        │                │
              │                        │ ·生成最终答案  │
              │                        │ ·事实提取      │
              │                        │ ·生命周期管理  │
              │                        │ ·RL DQN 决策   │
              │                        │ ·重规划判断    │
              │                        └───────┬────────┘
              │                                │
              │                    ┌───────────┼───────────┐
              │                    │                       │
              │             confidence ≥ 阈值        confidence < 阈值
              │                    │                       │
              │                    │              ┌────────▼────────┐
              │                    │              │  Replan Request  │
              │                    │              │  用户选择:       │
              │                    │              │  Continue/Adjust │
              │                    │              │  /Abort          │
              │                    │              └────────┬────────┘
              │                    │                       │
              │                    │              Adjust → 回到 PLAN
              │                    │
              │                    ▼
     ┌────────▼─────────────────────────────────────────────┐
     │                   后续处理                             │
     │                                                       │
     │  1. Stream 最终答案到 Channel                         │
     │  2. 持久化 Session 到 SQLite                          │
     │  3. 保存用户消息到 Memory                             │
     │  4. RL Episode 记录 (异步)                            │
     └───────────────────────────────────────────────────────┘
```

## 二、记忆系统数据流

```
┌─────────────────────────────────────────────────────────────┐
│                     记忆数据生命周期                          │
│                                                              │
│  用户消息 ──▶ 事实提取 (LLM)                                │
│                  │                                           │
│                  ▼                                           │
│           ┌─────────────┐                                   │
│           │ 生命周期决策 │ (LLM)                             │
│           │ ADD/UPDATE/  │                                   │
│           │ DELETE/NOOP  │                                   │
│           └──────┬──────┘                                   │
│                  │                                           │
│           ┌──────▼──────────────────────────────────┐       │
│           │        Markdown 文件写入                 │       │
│           │   ~/.ironclaw/memory/session/xxx.md     │       │
│           └──────┬──────────────────────────────────┘       │
│                  │                                           │
│           ┌──────▼──────┐                                   │
│           │ SQLite 索引 │                                   │
│           │ FTS5 + 向量 │                                   │
│           └──────┬──────┘                                   │
│                  │                                           │
│    ┌─────────────┼─────────────┬──────────────┐             │
│    ▼             ▼             ▼              ▼             │
│ ┌──────┐   ┌──────────┐  ┌────────┐   ┌──────────┐        │
│ │反射  │   │遗忘曲线  │  │会话整合│   │知识图谱  │        │
│ │L1/L2 │   │强度衰减  │  │session │   │实体提取  │        │
│ │      │   │自动归档  │  │→ user  │   │GraphSync │        │
│ └──────┘   └──────────┘  └────────┘   └──────────┘        │
│                                                              │
│  24h 周期        24h 周期      24h 检查      实时同步       │
└─────────────────────────────────────────────────────────────┘
```

## 三、知识库数据流

```
文件/目录
    │
    ▼
IngestPipeline
    │
    ├── 类型检测 (text/markdown/code/pdf/web)
    ├── 解析
    ├── 分块 (chunk_size=512, overlap=64)
    ├── 嵌入向量化
    └── 存入 SQLite (kb_chunks + kb_chunks_fts + kb_embeddings)
         │
         ├── 实体提取 → Knowledge Graph
         │
         └── 查询时:
              │
              ├── BM25 检索 (FTS5)
              ├── 向量检索 (余弦相似度)
              ├── RRF 融合
              ├── LLM 重排 (optional)
              └── 返回 Top-K 结果
```

## 四、RL 数据流

```
认知循环一轮
    │
    ├── PERCEIVE → buildInitialRLState()
    │
    ├── PLAN → PPO.SelectPlanStrategy()
    │          updateRLStateWithPlan()
    │
    ├── ACT → Bandit.SelectTool()
    │         Bandit.UpdateToolSelection()
    │         EpisodeCollector.Add(experience)
    │
    ├── OBSERVE → updateRLStateWithObservation()
    │
    ├── REFLECT → DQN.SelectReplanAction()
    │
    └── 循环结束:
        │
        ├── computeEpisodeReward()
        ├── Trainer.RecordEpisode()  ← 异步
        └── Trainer.AddExperience()  ← 异步
             │
             └── 后台训练:
                 ├── PPO 更新 (clip objective)
                 ├── DQN 更新 (target network)
                 └── 检查点保存 (SQLite)
```

## 五、模块依赖关系图

```
                    ┌─────────┐
                    │ config  │
                    └────┬────┘
                         │
                    ┌────▼────┐
                    │ gateway │
                    └────┬────┘
                         │
         ┌───────────────┼───────────────┐
         │               │               │
    ┌────▼────┐    ┌─────▼────┐    ┌────▼────┐
    │  agent  │    │  memory  │    │ channel │
    └────┬────┘    └────┬─────┘    └─────────┘
         │              │
    ┌────┼────┐    ┌────┼─────┐
    │    │    │    │    │     │
┌───▼┐ ┌▼──┐┌▼──┐│ ┌──▼──┐  │
│tool│ │rl ││mcp││ │know-│  │
└────┘ └───┘└───┘│ │ledge│  │
                  │ └──┬──┘  │
                  │    │     │
                  │ ┌──▼──┐  │
                  │ │graph│  │
                  │ └─────┘  │
                  │          │
             ┌────▼────┐  ┌─▼────┐
             │ session  │  │store │
             └──────────┘  └──────┘
```

## 六、并发模型

```
主 Goroutine
    │
    ├── Gateway.Start()
    │
    ├── Channel Goroutine(s)
    │   ├── Telegram 长轮询
    │   └── TUI Bubble Tea 主循环
    │
    ├── Scheduler Goroutine
    │   └── 定期轮询任务
    │
    ├── MCP Watcher Goroutine
    │   └── 30s 轮询 ~/.IronClaw/mcp/
    │
    ├── Forgetting Curve Goroutine
    │   └── 24h 周期衰减+归档
    │
    ├── Graph Decay Goroutine
    │   └── 24h 周期关系衰减
    │
    ├── Entity Extraction Goroutine (启动时)
    │   └── 初始知识库→图谱提取
    │
    ├── Compactor Goroutine
    │   └── 6h 周期记忆压缩
    │
    ├── RL Trainer Goroutine
    │   └── 后台训练循环
    │
    └── 消息处理 Goroutine (per message)
        └── handleInbound → Agent 循环
            └── RL Episode 记录 Goroutine (fire-and-forget)
```

## 七、关键设计原则

| 原则 | 体现 |
|------|------|
| **Local-first** | SQLite + 本地文件，无外部依赖 |
| **渐进增强** | 每个子系统可独立启用/禁用 |
| **优雅降级** | 无 API key/FTS5 时自动降级 |
| **松耦合** | 接口适配器隔离模块间依赖 |
| **文件可读** | Markdown 记忆、YAML 配置、SKILL.md |
| **安全优先** | 命令黑名单 + 工具审批 + 隐私控制 |
| **仿生设计** | 遗忘曲线 + 记忆整合 + 反射 + 认知循环 |
